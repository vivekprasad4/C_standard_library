#include <stdio.h>
#include "example.h"

int main(void)
{
        hello("World");
        printf("%d\n", Double(333));
        CFunc();
        //CppFunc();

        //MyClass a;
        //a.func();

        return 0;
}